Alert:
  type: object
  properties:
    id: { type: string }
    deviceId: { type: string }
    type: { type: string }
    message: { type: string }
    createdAt: { type: integer, format: int64 }
    acknowledged: { type: boolean }